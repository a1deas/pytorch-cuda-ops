services:
  app:
    image: pytorch-cuda-ops:dev
    container_name: pytorch-cuda-ops
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - .:/workspace
      - ~/.cache/pip:/root/.cache/pip
    working_dir: /workspace
    tty: true
    stdin_open: true
